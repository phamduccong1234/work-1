const person = {
  firstName: "John",
  lastName: "Doe",
  age: 30,
  occupation: "Software Engineer",
};

const { firstName, lastName, age } = person;
console.log(firstName + " " + lastName + " " + age);
